# Test sources

Tests generate synthetic rows using the exact headers in `schemas.json`. Candidate names are generic examples; no personal records were copied into fixtures. The cases exercise all six office schemas, source category distinctions, leading zeroes, repeated records across batches, missing names, and interrupted writes.

The published CSVs document the historical collection, but no complete source-request manifest or capture times are available. No live requests were made to create these tests.
